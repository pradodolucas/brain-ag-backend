describe.skip('removed tests', () => {});

import { describe, it, expect } from '@jest/globals';

describe('App (placeholder)', () => {
	it('teste placeholder para evitar arquivo vazio', () => {
		expect(true).toBeTruthy();
	});
});
